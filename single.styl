*
    font-family sans-serif
    font-size inherit
    margin 0
    padding 0
    border 0
    box-sizing border-box
    
#back
    font-size 3vmax
    width 7vmax
    height 7vmax
    color #000
    background #fff
    border-radius 50%
    position absolute
    bottom 5vmax
    display: flex;
    justify-content: center;
    align-items: center;
    text-decoration none
    box-shadow: 0 25px 25px 0 rgba(0, 0, 0, .7);
    animation fadeIn .3s ease-in forwards
body
    width 100%
    height 100vh
    display: flex;
    justify-content: center;
    align-items: center;
    position relative
    background #000
    opacity: 1
    transform:scale(1)
img,video
    width:auto;
    height:auto;
    max-width:100%;
    max-height:90%;
    animation fadeIn .3s ease-in forwards
iframe
    width 100%
    height 100%
    animation fadeIn .3s ease-in forwards
.open
    animation fadeOut .3s ease-in forwards
@keyframes fadeIn
    0%
        opacity: 0
        transform:scale(0);
    100%
        opacity 1
        transform:scale(1);
@keyframes fadeOut
    0%
        opacity: 1
        transform:scale(1)
    100%
        opacity 0
        transform:scale(2)